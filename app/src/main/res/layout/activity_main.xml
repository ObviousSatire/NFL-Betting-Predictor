<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="#121212">

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🏈 NFL BETTING"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:padding="4dp"
        android:layout_marginBottom="4dp" />

    <!-- Team Dropdown -->
    <Spinner
        android:id="@+id/teamSpinner"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="4dp"
        android:background="@android:drawable/btn_dropdown"
        android:padding="8dp"
        android:visibility="visible" />

    <!-- Player Dropdown -->
    <Spinner
        android:id="@+id/playerSpinner"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="4dp"
        android:background="@android:drawable/btn_dropdown"
        android:padding="8dp"
        android:visibility="gone" />

    <!-- Main Content Area -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="4dp"
        android:background="#1E1E1E"
        android:padding="8dp">

        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select a team to begin..."
            android:textSize="13sp"
            android:textColor="#E0E0E0"
            android:fontFamily="monospace"
            android:lineSpacingExtra="2dp" />
    </ScrollView>

    <!-- Buttons Row 1 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button android:id="@+id/testButton" android:layout_width="80dp" android:layout_height="40dp" android:text="TEST" android:textSize="12sp" android:layout_marginEnd="2dp" android:backgroundTint="#607D8B" android:padding="0dp" />
        <Button android:id="@+id/loadStatsButton" android:layout_width="80dp" android:layout_height="40dp" android:text="STATS" android:textSize="12sp" android:layout_marginEnd="2dp" android:backgroundTint="#4CAF50" android:padding="0dp" />
        <Button android:id="@+id/loadPlayerButton" android:layout_width="85dp" android:layout_height="40dp" android:text="PLAYER" android:textSize="12sp" android:layout_marginEnd="2dp" android:backgroundTint="#FF9800" android:padding="0dp" />
        <Button android:id="@+id/predictButton" android:layout_width="85dp" android:layout_height="40dp" android:text="PREDICT" android:textSize="12sp" android:backgroundTint="#9C27B0" android:padding="0dp" />

    </LinearLayout>

    <!-- Buttons Row 2 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="2dp"
        android:gravity="center">

        <Button android:id="@+id/refreshAllButton" android:layout_width="90dp" android:layout_height="40dp" android:text="REFRESH" android:textSize="12sp" android:layout_marginEnd="2dp" android:backgroundTint="#2196F3" android:padding="0dp" />
        <Button android:id="@+id/clearButton" android:layout_width="80dp" android:layout_height="40dp" android:text="CLEAR" android:textSize="12sp" android:layout_marginEnd="2dp" android:backgroundTint="#9E9E9E" android:padding="0dp" />
        <Button android:id="@+id/helpButton" android:layout_width="80dp" android:layout_height="40dp" android:text="HELP" android:textSize="12sp" android:backgroundTint="#FF5722" android:padding="0dp" />

    </LinearLayout>

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/loadingIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="4dp"
        android:visibility="gone" />

</LinearLayout>